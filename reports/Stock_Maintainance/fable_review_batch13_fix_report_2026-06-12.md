# Fable 审计修复报告 Batch 13

生成时间：2026-06-12

## 修复范围

本批次处理 M26：交易日历不可用时，planner 将交易日读取窗口直接当作日历日回退，导致长窗口上下文不足。

## 已修复问题

| 审计项 | 判断 | 修复结论 |
|---|---|---|
| M26 planner 回退路径对 5 年字段读取上下文不足 | 客观 | 当交易日历不可用时，读取上下文按 `ceil(交易日窗口 * 7 / 5) + 14` 转换为日历天 |

## 行为说明

该修复只扩大读取上下文，不扩大写入窗口。日批在交易日历缺失时仍保持保守写入范围，但长窗口字段不再因回退路径少读约 400 个交易日。

## 验证

| 命令 | 结果 |
|---|---|
| `.venv-wsl/bin/pytest -q tests/test_feature_planner.py` | 3 passed |
| `.venv-wsl/bin/pytest -q` | 77 passed |

